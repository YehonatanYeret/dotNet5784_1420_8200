<Window x:Class="PL.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="mainWindow" Height="450" Width="800"
        ResizeMode="NoResize"
        DataContext="{Binding CurrentUser, RelativeSource={RelativeSource Mode=Self}, Mode=TwoWay}"
        Background="{StaticResource backgroundImage}">


    <Grid>

        <!-- TextBlock displaying project name -->
        <TextBlock Panel.ZIndex="2" VerticalAlignment="Top" HorizontalAlignment="Center" Height="119" FontFamily="Impact" FontSize="46" FontWeight="Bold" FontStyle="Italic" Margin="0,31,0,0" Width="323">
            <Run Text="Project "/>
            <LineBreak/>
            <Run Text="   Managment"/>
        </TextBlock>

        <!-- Grid containing buttons -->
        <Grid Panel.ZIndex="1" VerticalAlignment="Bottom" Margin="400,0,20,90">

            <Grid.RowDefinitions>
                <RowDefinition Height="4*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">

                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Label Grid.Row="0" Grid.Column="0" Content="Email:"/>
                <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Email, Mode=TwoWay}"/>

                <Label Grid.Row="1" Grid.Column="0" Content="Password:"/>
                <PasswordBox Grid.Row="1" Grid.Column="1" PasswordChanged="PasswordBox_PasswordChange"/>

            </Grid>

            <Button Grid.Row="1" Content="Enter" Click="BtnEnterUser_Click"/>

        </Grid>
    </Grid>
</Window>

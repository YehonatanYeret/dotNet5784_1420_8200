<Window x:Class="PL.Manager.GantWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL.Manager"
        mc:Ignorable="d"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}"
        Title="GantWindow" Height="450" Width="800"
        SizeToContent="Height">
    <Grid Margin="15">
        <Border BorderBrush="Black" BorderThickness="2">

            <ItemsControl ItemsSource="{Binding GantData}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Vertical"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="150"/>
                                    <ColumnDefinition Width="3"/>
                                    <ColumnDefinition Width="auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{Binding Alias}" FontWeight="SemiBold"
                                   FontFamily="ariel" Grid.Column="0" 
                                       TextWrapping="WrapWithOverflow" Background="LightCoral" TextAlignment="Center" Padding="3"/>

                                <Border Grid.Column="1" VerticalAlignment="Stretch" Width="3" Background="Black" />
                                <Grid Background="LightGray" Grid.Column="2" Margin="0,1">
                                    <Rectangle Width="{Binding RequiredEffortTime, Converter={StaticResource ConvertEffortTimeToWidthKey}}"
                                   Margin="{Binding ScheduledDate, Converter={StaticResource ConvertStartDateToMarginKey}}" Height="20"
                                   Stroke="Black"
                                   Fill="LightBlue"
                                   >
                                    </Rectangle>
                                </Grid>
                            </Grid>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Border>
    </Grid>
</Window>
